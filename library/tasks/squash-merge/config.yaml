description: Squash a branch into the target and land on it.
args:
  - name: source
    description: branch to squash
  - name: target
    default: main
dedup: none
worktree: repo
model: opus
timeout: 30m
